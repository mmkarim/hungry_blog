<% provide :title, t("home") %>
<%= render partial: "shared/sidebar" %>

<% @posts.each do |post| %>
  <%= render partial: "posts/post", locals: {post: post} %>
<% end %>

<%= render partial: "shared/paginate", locals: {results: @posts} %>
